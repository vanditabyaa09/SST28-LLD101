/**
 * Enum to represent the state of a pen
 */
public enum PenState {
    CLOSED,    // Pen cap is on, cannot write
    OPEN,      // Pen cap is off, ready to write
    WRITING    // Pen is actively writing
}
